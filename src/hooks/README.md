# 🪝 Custom Hooks

Esta pasta contém hooks personalizados reutilizáveis.

## 📋 Hooks Planejados

- `useAuth` - Hook para autenticação (pode extrair do AuthContext)
- `useCart` - Hook para carrinho (pode extrair do CartContext)
- `useLocalStorage` - Hook para persistência local
- `useDebounce` - Hook para debounce de inputs
- `useMediaQuery` - Hook para responsividade

## 📝 Nota

Atualmente, a lógica de autenticação e carrinho está nos Contexts. 
No futuro, podemos extrair hooks personalizados para melhor reutilização.

